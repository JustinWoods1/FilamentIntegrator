from gpiozero import MCP3008
import time
while(1):
  adc0=MCP3008(channel=0, device=0)
  adc1=MCP3008(channel=1, device=0)

  print('{:.2f}'.format(adc0.value))
  print("Weight on Scale: %.2f, %.2f" %(adc0.value*1023, adc1.value*1023))
  time.sleep(0.5)
